============
Installation
============

At the command line::

    $ easy_install suit_sortable

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv suit_sortable
    $ pip install suit_sortable